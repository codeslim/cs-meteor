<%= groupVar %>.route('<%= path %>', {
  name: '<%= name %>',
  action(params, queryParams) {
    <%= front %>Layout.render('<%= layout %>', {
      yield: '<%= template %>'<% args.forEach(arg => { -%><%= `,\n      ${arg}: params.${arg}` %><% }) %>
    })
  }
});
