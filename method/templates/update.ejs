Meteor.methods({
  update<%= collection %>(modifier, id) {
    check(modifier, Object);

    if (!Meteor.userId()) {
      throw new Meteor.Error("not-authorized");
    }
    
    try {
      return Collections.<%= collection %>.update(id, modifier);
    } catch(exception) {
      return exception;
    }
  }
});
