Meteor.methods({
  remove<%= collection %>(doc) {
    check(doc, Mongo.ObjectID);

    <% if (userSystem) { %>
      <%- include(userSystem) %>
    <% } %>

    try {
      return Collections.<%= collection %>.remove(doc);
    } catch(exception) {
      return exception;
    }
  }
});
