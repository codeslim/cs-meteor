Meteor.methods({
  add<%= collection %>(doc) {
    check(doc, Schemas.<%= collection %>);

    if (!Meteor.userId()) {
      throw new Meteor.Error("not-authorized");
    }

    try {
      return Collections.<%= collection %>.insert(doc);
    } catch(exception) {
      return exception;
    }
  }
});
