Meteor.methods({
  add<%= collection %>(doc) {
    check(doc, Schemas.<%= collection %>);

    <% if (userSystem) { %>
      <%- include(userSystem) %>
    <% } %>
    
    try {
      return Collections.<%= collection %>.insert(doc);
    } catch(exception) {
      return exception;
    }
  }
});
